Run the Script: When you’re ready to run the script on any machine, activate the venv environment:

First run:
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows

Then, run:
python wifi_speed_logger.py